diff --git a/public/index.html b/public/index.html
index 92dbf7d..cfa2503 100644
--- a/public/index.html
+++ b/public/index.html
@@ -2963,7 +2963,7 @@ button:focus-visible{
 }
 
 .name-input::placeholder{
-  color:var(--dim2);
+  color:rgba(var(--overlay),.55);
 }
 
 .name-input:focus{
@@ -3380,27 +3380,55 @@ button:focus-visible{
 .auth-link{
   background:transparent;
 
-  border:0;
+  border:1px solid var(--line);
 
-  color:var(--dim);
+  border-radius:12px;
+
+  color:var(--ink);
 
   cursor:pointer;
 
+  display:block;
+
   font-family:'Karla',sans-serif;
 
-  font-size:12px;
+  font-size:13px;
 
-  font-weight:600;
+  font-weight:700;
 
-  padding:0;
+  padding:11px 18px;
 
-  text-decoration:underline;
+  text-align:center;
+
+  text-decoration:none;
+
+  width:100%;
 }
 
 .auth-link:hover{
+  border-color:rgba(var(--overlay),.3);
+
   color:var(--ink);
 }
 
+.auth-hint{
+  color:var(--dim);
+
+  font-size:12px;
+
+  line-height:1.4;
+
+  margin-top:-6px;
+}
+
+.auth-input-optional{
+  opacity:.72;
+}
+
+.auth-input-optional:focus{
+  opacity:1;
+}
+
 .auth-msg{
   border-radius:11px;
 
@@ -10067,7 +10095,7 @@ src="https://www.facebook.com/tr?id=4385853084997712&ev=PageView&noscript=1"
           <button
             class="name-edit"
             id="nameEdit">
-            Kambia
+            Kambia nòmber
           </button>
 
           <button
@@ -10129,7 +10157,7 @@ src="https://www.facebook.com/tr?id=4385853084997712&ev=PageView&noscript=1"
         </button>
       </div>
       <input
-        class="name-input"
+        class="name-input auth-input-optional"
         id="authEmail"
         type="email"
         autocomplete="email"
@@ -10142,11 +10170,17 @@ src="https://www.facebook.com/tr?id=4385853084997712&ev=PageView&noscript=1"
         type="password"
         autocomplete="new-password"
         placeholder="Kontraseña">
+      <div
+        class="auth-hint"
+        id="authPasswordHint">
+        Mínimo 6 karakter.
+      </div>
 
       <button
         class="auth-submit"
         id="authSubmit"
-        type="submit">
+        type="submit"
+        disabled>
         Krea kuenta
       </button>
 
@@ -40226,7 +40260,38 @@ function setAuthBusy(busy){
 
   authBusy = busy;
 
-  $('authSubmit').disabled = busy;
+  if(busy){
+    $('authSubmit').disabled = true;
+  }else{
+    updateAuthSubmitState();
+  }
+
+}
+
+
+/*
+  Keeps the submit button visibly inactive until the form can
+  actually succeed, instead of looking clickable while it will
+  just bounce back with a validation error.
+*/
+
+function updateAuthSubmitState(){
+
+  const username =
+    $('authUsername').value.trim();
+
+  const password =
+    $('authPassword').value;
+
+  const passwordOk =
+    authMode === 'signup'
+      ? password.length >= 6
+      : password.length > 0;
+
+  $('authSubmit').disabled =
+    authBusy ||
+    !username ||
+    !passwordOk;
 
 }
 
@@ -40268,6 +40333,11 @@ function renderAuth(){
     signup
       ? 'Bo tin kuenta kaba? Drenta'
       : 'No tin kuenta ainda? Krea un';
+  $('authPasswordHint').style.display =
+    signup
+      ? ''
+      : 'none';
+  updateAuthSubmitState();
 }
 
 
@@ -40314,6 +40384,18 @@ async function submitAuth(username,password,email){
 }
 const EMAIL_PATTERN_AUTH =
   /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
+if($('authUsername')){
+  $('authUsername').addEventListener(
+    'input',
+    updateAuthSubmitState
+  );
+}
+if($('authPassword')){
+  $('authPassword').addEventListener(
+    'input',
+    updateAuthSubmitState
+  );
+}
 if($('authUsername')){
   $('authUsername').addEventListener(
     'blur',
